<Window
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:transcriptor"
        xmlns:System="clr-namespace:System;assembly=mscorlib" x:Class="transcriptor.MainWindow"
        mc:Ignorable="d"
        Title="transcriptor" Width="400" Height="320" ResizeMode="CanMinimize">
    <Window.Resources>
        <SolidColorBrush x:Key="Button.Static.Background" Color="#FFFFFF"/>
        <Color x:Key="Button.Static.Background.Color">#FFFFFF</Color>
        <SolidColorBrush x:Key="Button.Static.Border" Color="#FFE0E0E0"/>
        <Color x:Key="Button.MouseOver.Background.Color">#FFE0E0E0</Color>
        <Style TargetType="{x:Type Button}">
            <Setter Property="Background" Value="{StaticResource Button.Static.Background}"/>
            <Setter Property="BorderBrush" Value="{StaticResource Button.Static.Border}"/>
            <Setter Property="Foreground" Value="{DynamicResource {x:Static SystemColors.ControlTextBrushKey}}"/>

            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type Button}">
                        <Border x:Name="border" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" Background="{TemplateBinding Background}" SnapsToDevicePixels="true">
                            <ContentPresenter x:Name="contentPresenter" Focusable="False" HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}" Margin="{TemplateBinding Padding}" RecognizesAccessKey="True" SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}" VerticalAlignment="{TemplateBinding VerticalContentAlignment}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="true">
                                <Trigger.EnterActions>
                                    <BeginStoryboard>
                                        <Storyboard>
                                            <ColorAnimation Storyboard.TargetName="border" Storyboard.TargetProperty="Background.Color" 
                                                            From="{StaticResource Button.Static.Background.Color}" 
                                                            To="{StaticResource Button.MouseOver.Background.Color}"           
                                                            Duration="0:0:0.25"
                                                            AutoReverse="False"/>
                                        </Storyboard>
                                    </BeginStoryboard>
                                </Trigger.EnterActions>

                                <Trigger.ExitActions>
                                    <BeginStoryboard>
                                        <Storyboard>
                                            <ColorAnimation Storyboard.TargetName="border" Storyboard.TargetProperty="Background.Color" 
                                                            From="{StaticResource Button.MouseOver.Background.Color}"           
                                                            To="{StaticResource Button.Static.Background.Color}" 
                                                            Duration="0:0:0.25"
                                                            AutoReverse="False"/>
                                        </Storyboard>
                                    </BeginStoryboard>
                                </Trigger.ExitActions>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>
    <Grid Margin="10">
        <TextBox x:Name="text" Margin="10,10,10,52" TextWrapping="Wrap" Text="Enter text" RenderTransformOrigin="0.5,0.5" TextChanged="Text_TextChanged">
            <TextBox.RenderTransform>
                <TransformGroup>
                    <ScaleTransform/>
                    <SkewTransform/>
                    <RotateTransform/>
                    <TranslateTransform/>
                </TransformGroup>
            </TextBox.RenderTransform>
        </TextBox>
        <Button x:Name="but" Content="выполнить" Margin="214,230,0,0" Width="150" Click="But_Click_1" Height="31" HorizontalContentAlignment="Center" VerticalAlignment="Center" HorizontalAlignment="Center"/>
        <ComboBox x:Name="combo" HorizontalAlignment="Left" Margin="10,239,0,0" VerticalAlignment="Top" Width="120" IsSynchronizedWithCurrentItem="True" SelectedIndex="1">
            <Button Content="ru-&gt;en" Background="{x:Null}" IsEnabled="False" Foreground="Black"/>
            <Button Content="en-&gt;ru" Background="{x:Null}" IsEnabled="False" Foreground="Black"/>
        </ComboBox>

    </Grid>
</Window>
