<Window
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:transcriptor"
        xmlns:System="clr-namespace:System;assembly=mscorlib" xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes" x:Class="transcriptor.MainWindow"
        mc:Ignorable="d"
    TextElement.Foreground="{DynamicResource MaterialDesignBody}"
     TextElement.FontWeight="Regular"
     TextElement.FontSize="13"
     TextOptions.TextFormattingMode="Ideal"
     TextOptions.TextRenderingMode="Auto"
     Background="{DynamicResource MaterialDesignPaper}"
     FontFamily="{DynamicResource MaterialDesignFont}"
        Title="transcriptor" Width="400" Height="356" ResizeMode="CanMinimize" Icon="favicon.ico">
    


    <Grid Margin="10">
        <Button x:Name="but" Content="Выполнить" Margin="0,263,10,0" Width="120" Click="But_Click_1" Height="32" HorizontalContentAlignment="Center" HorizontalAlignment="Right" VerticalAlignment="Top" VerticalContentAlignment="Center"/>
        <ComboBox x:Name="combo" Margin="10,263,0,0" Width="120" IsSynchronizedWithCurrentItem="True" SelectedIndex="1" VerticalContentAlignment="Center" HorizontalContentAlignment="Center" HorizontalAlignment="Left" VerticalAlignment="Top" Height="32" BorderBrush="Black" Foreground="Black">
            <ComboBox.Background>
                <LinearGradientBrush EndPoint="0,1" StartPoint="0,0">
                    <GradientStop Color="#FFF0F0F0" Offset="0"/>
                    <GradientStop Color="White" Offset="1"/>
                </LinearGradientBrush>
            </ComboBox.Background>
            <ComboBoxItem Content="ru->en"/>
            <ComboBoxItem Content="en->ru"/>
        </ComboBox>
        <Label x:Name="label" Content="Символов: " HorizontalAlignment="Left" Margin="244,235,0,0" VerticalAlignment="Top" Width="120" Height="23" VerticalContentAlignment="Center"/>
        <TextBox x:Name="text"
    Style="{StaticResource MaterialDesignOutlinedTextFieldTextBox}"
    VerticalAlignment="Top"
    Height="229"
    AcceptsReturn="True"
    TextWrapping="Wrap"
    VerticalScrollBarVisibility="Auto"
    materialDesign:HintAssist.Hint="Вставьте эльфийский сюда"
    IsEnabled="{Binding Path=IsChecked, ElementName=MaterialDesignOutlinedTextFieldTextBoxEnabledComboBox}" />
        <ToggleButton x:Name="toggle"
  Style="{StaticResource MaterialDesignSwitchToggleButton}"
  ToolTip="Default ToggleButton Style" Margin="10,235,319,40" IsChecked="True" />
        <Label Content="Удалять другой язык" HorizontalAlignment="Left" Margin="60,234,0,0" VerticalAlignment="Top" Height="24"/>
    </Grid>

</Window>
